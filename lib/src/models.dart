// Export the TerminalController class for backward compatibility
export 'terminal_controller.dart' show TerminalController;

// Note: terminal_types.dart is already exported by terminal_controller.dart
// New code should import directly from terminal_controller.dart
